export enum STATUS {
    ACTIVE = 1,
    INACTIVE = 2
}

export enum POPULAR {
    ACTIVE = 1,
    INACTIVE = 2
}

export enum PAGE {
    CURRENT = 0,
    SIZE = 10
}

export enum API {
    CATEGORY = 'category',
    ADMINS = 'admins'
}

export enum ROLE {
    ADMINS = 1
}
